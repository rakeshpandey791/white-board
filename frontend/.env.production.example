# Frontend production env template (Vite)
# These values are used at build time.

# Backend API base URL (must include /api)
VITE_API_BASE_URL=https://whiteboard-backend.onrender.com/api

# Backend WebSocket endpoint (SockJS)
VITE_WS_ENDPOINT=https://whiteboard-backend.onrender.com/ws
